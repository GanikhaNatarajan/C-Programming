#include<stdio.h>
int main() {
    int hours;
    scanf("%d",&hours);
    if (hours<=1 ) {
        printf("Short interruption");
    }else if (hours<=4) {
        printf("Medium interruption");
    }else {
        printf("Long interruption");
    }
    return 0;
}