#include<stdio.h>
int main()
{
    int n;
    int total=0;
    scanf("%d",&n);
    for(int i=1;i<=n;i++) {
        int ticket=0;;
        scanf("%d",&ticket);
        total+=ticket;
    }
    printf("Total Collection ; %d\n",total );
    return 0;
}